---
- name: Configuration des serveurs MediTrack
  hosts: all
  become: true
  tasks:
    - name: Installer nginx
      apt:
        name: nginx
        state: present
      when: ansible_distribution == 'Ubuntu'

    - name: Démarrer nginx
      service:
        name: nginx
        state: started
